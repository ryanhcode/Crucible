package dev.foundry.crucible.extension;

import net.minecraft.core.BlockPos;

public interface LevelDuck {

    BlockPos.MutableBlockPos crucible$getBlockRandomPos(int x, int y, int z, int l, BlockPos.MutableBlockPos set);
}
